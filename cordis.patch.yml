# AUTO-GENERATED by scripts/aggregate.mjs -- do not edit.
# Source of truth: aggregate.yml (pnpm generate).
#
# Aggregates 13 DSH plugins into one bundle. Suite rows use
# the "aio-" ID namespace because identical IDs across two mounted bundles
# are boot-fatal. For every plugin, the suite also emits a conditional
# disable of the standalone row ID: applied LAST (dsh plugin add appends
# bundles last), it supersedes enabled standalone mounts; with the suite
# removed, the standalone rows re-enable themselves. Profile-patch config
# overrides should target the "aio-" IDs to tune suite-provided plugins.

# Manifest extra rows (conditional on their required plugin being enabled).
- id: session-title-llm
  disabled: true

# Supersede enabled standalone mounts (suite layer applies last).
- id: dsh-coding-tools
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'dsh-coding-tools' && !entry.disabled)"

- id: dsh-plugin-manager
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'dsh-plugin-manager' && !entry.disabled)"

- id: config-manager
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'config-manager' && !entry.disabled)"

- id: routed-subagent
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'routed-subagent' && !entry.disabled)"

- id: dsh-status-bar-config
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'dsh-status-bar-config' && !entry.disabled)"

- id: workspace-git
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'workspace-git' && !entry.disabled)"

- id: better-sidebar
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'better-sidebar' && !entry.disabled)"

- id: auto-chat-titles
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'auto-chat-titles' && !entry.disabled)"

- id: claude-usage
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'claude-usage' && !entry.disabled)"

- id: codex-usage
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'codex-usage' && !entry.disabled)"

- id: harness-updater
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'harness-updater' && !entry.disabled)"

- id: dsh-image-preview
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'dsh-image-preview' && !entry.disabled)"

- id: dsh-companion
  disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.id === 'dsh-companion' && !entry.disabled)"

- insert:
    - id: aio-dsh-coding-tools
      name: dsh-coding-tools

- insert:
    - id: aio-dsh-plugin-manager
      name: '@leonardoxr/dsh-plugin-manager'

- insert:
    - id: aio-config-manager
      name: dsh-config-manager

- insert:
    - id: aio-routed-subagent
      name: dsh-routed-subagent

- insert:
    - id: aio-dsh-status-bar-config
      name: dsh-status-bar-config

- insert:
    - id: aio-workspace-git
      name: dsh-workspace-git

- insert:
    - id: aio-better-sidebar
      name: dsh-better-sidebar

- insert:
    - id: aio-auto-chat-titles
      name: dsh-auto-chat-titles

- insert:
    - id: aio-claude-usage
      name: dsh-claude-usage

- insert:
    - id: aio-codex-usage
      name: dsh-codex-usage

- insert:
    - id: aio-harness-updater
      name: dsh-harness-updater

- insert:
    - id: aio-dsh-image-preview
      name: dsh-image-preview

- insert:
    - id: aio-dsh-companion
      name: dsh-companion
