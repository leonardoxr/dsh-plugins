# AUTO-GENERATED by scripts/aggregate.mjs -- do not edit.
# Source of truth: aggregate.yml (pnpm generate).
#
# Aggregates 12 DSH plugins into one bundle row set. IDs are each
# plugin's canonical standalone ID so profile-patch config overrides keep
# working after migration. This suite REPLACES the standalone bundles:
# installing both is boot-fatal by loader design (duplicate row IDs). Every
# insert also carries a !!js back-off guard against earlier same-package
# mounts under a different ID; guards only see earlier entries, and
# "dsh plugin add" appends this bundle last.

# Manifest extra rows (conditional on their required plugin being enabled).
- id: session-title-llm
  disabled: true

- insert:
    - id: dsh-coding-tools
      name: dsh-coding-tools
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-coding-tools' && entry.options.id !== 'dsh-coding-tools' && !entry.disabled)"

- insert:
    - id: dsh-plugin-manager
      name: '@leonardoxr/dsh-plugin-manager'
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === '@leonardoxr/dsh-plugin-manager' && entry.options.id !== 'dsh-plugin-manager' && !entry.disabled)"

- insert:
    - id: routed-subagent
      name: dsh-routed-subagent
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-routed-subagent' && entry.options.id !== 'routed-subagent' && !entry.disabled)"

- insert:
    - id: dsh-status-bar-config
      name: dsh-status-bar-config
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-status-bar-config' && entry.options.id !== 'dsh-status-bar-config' && !entry.disabled)"

- insert:
    - id: workspace-git
      name: dsh-workspace-git
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-workspace-git' && entry.options.id !== 'workspace-git' && !entry.disabled)"

- insert:
    - id: better-sidebar
      name: dsh-better-sidebar
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-better-sidebar' && entry.options.id !== 'better-sidebar' && !entry.disabled)"

- insert:
    - id: auto-chat-titles
      name: dsh-auto-chat-titles
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-auto-chat-titles' && entry.options.id !== 'auto-chat-titles' && !entry.disabled)"

- insert:
    - id: claude-usage
      name: dsh-claude-usage
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-claude-usage' && entry.options.id !== 'claude-usage' && !entry.disabled)"

- insert:
    - id: codex-usage
      name: dsh-codex-usage
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-codex-usage' && entry.options.id !== 'codex-usage' && !entry.disabled)"

- insert:
    - id: harness-updater
      name: dsh-harness-updater
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-harness-updater' && entry.options.id !== 'harness-updater' && !entry.disabled)"

- insert:
    - id: dsh-image-preview
      name: dsh-image-preview
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-image-preview' && entry.options.id !== 'dsh-image-preview' && !entry.disabled)"

- insert:
    - id: dsh-companion
      name: dsh-companion
      disabled: !!js "[...ctx.loader.entries()].some((entry) => entry.options.name === 'dsh-companion' && entry.options.id !== 'dsh-companion' && !entry.disabled)"
